# 背景 / 目标

用户要求去掉“图形选股”和“统一池”模块。目标是在不回滚现有工作区其他改动的前提下，移除对应前端入口、页面内容、轮询/请求逻辑、后端 API 挂载和服务依赖，并同步更新 README。

# 任务分解（checklist）

- [x] 清理后端图形选股和统一池 API 暴露。
- [x] 清理前端侧边栏、Tab、渲染逻辑、请求逻辑和样式。
- [x] 删除统一池服务文件，并保留首板 Alpha 训练/推理能力中与图形 Tab 无关的代码。
- [x] 更新 README 的功能、结构和 API 列表。
- [x] 执行可行测试与服务重启。
- [x] 提交并推送到 `origin/main`。

# 验收标准

- 页面不再展示“图形选股”和“统一池”入口。
- 前端不再请求 `/api/strategy/first-limit-alpha/graphic*` 和 `/api/unified-pool*`。
- 后端不再注册对应 API。
- README 不再把这两个模块列为现有功能/API。
- 相关测试通过，服务可启动。

验证记录：`python3 -m compileall app/main.py app/routers/first_limit_alpha.py app/services/hermes_runtime.py` 通过；`node --check app/static/app.js` 通过；图形/统一池关键字扫描无残留；`tests/test_api_regression.py` 在长链路外部接口测试中导致服务退出，需另行定位。

# 风险与回滚

- 当前工作区存在既有未提交改动，编辑时需要保留无关内容。
- 若删除图形 API 影响首板 Alpha 训练测试，应仅移除对外路由和页面入口，不删除底层模型代码。

# 关键决策记录

- “图形选股”按产品模块移除：删除 Tab/API，不删除 `strategy/first_limit_alpha` 训练资料。
- “统一池”按模块移除：删除服务实例、API、前端页面和相关 README 描述。
