# 背景 / 目标

数据中心“缺失日期（30天）”列表当前按较早日期在前展示，用户需要最近日期优先显示。

目标：不改后端统计逻辑，仅在前端渲染缺失日期列表时按日期倒序排列，让最近交易日在最上方。

# 任务分解（checklist）

- [ ] 定位缺失日期列表渲染逻辑。
- [ ] 将默认日期排序调整为倒序。
- [ ] 保留现有“按缺失条数排序”的切换逻辑。
- [ ] 执行前端语法检查。
- [ ] 按项目规则重启服务、提交并推送。

# 验收标准

- “缺失日期（30天）”默认最近日期在上。
- 点击排序切换到缺失条数时仍按缺失数量倒序。
- `node --check app/static/app.js` 通过。

# 风险与回滚

- 风险：仅影响展示顺序，不影响数据完整性统计。
- 回滚：恢复缺失日期列表使用接口原始顺序。

# 关键决策记录

- 本次只改前端展示排序，不调整后端报告接口。
