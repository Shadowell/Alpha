# 背景 / 目标

用户要求去掉页面中的“盘后筛选”及其相关模块。目标是在保留现有未提交改动的前提下，移除对应前端入口、交互逻辑、后端 API/调度/服务依赖，并同步更新 README。

# 任务分解（checklist）

- [x] 定位“盘后筛选”的前端入口、请求逻辑、后端接口和服务模块。
- [x] 删除前端按钮、状态展示、渲染逻辑和相关样式。
- [x] 清理后端 API、后台任务、服务实例和无用模块引用。
- [x] 更新 README 的功能/API/结构描述。
- [x] 执行可行测试并重启服务。
- [x] 提交并推送到 `origin/main`。

# 验收标准

- 页面不再展示“盘后筛选”按钮或入口。
- 前端不再调用盘后筛选相关 API。
- 后端不再暴露盘后筛选相关 API 或后台调度。
- README 不再把盘后筛选列为现有能力。
- 服务可启动，核心静态检查和相关测试通过。

验证记录：盘后筛选关键字/API/函数扫描无残留；`python3 -m compileall app/main.py app/mcp_server.py app/config.py app/services/funnel_service.py app/services/strategy_engine.py app/services/hermes_runtime.py` 通过；`node --check app/static/app.js` 通过；`pytest tests/test_transition_rules.py tests/test_custom_strategy.py tests/test_funnel_concepts.py tests/test_first_limit_alpha.py tests/test_kline_store.py tests/test_kline_cache_service.py` 通过；`./restart.sh` 成功启动服务，`POST /api/jobs/eod-screen` 返回 404。

# 风险与回滚

- 当前工作区已有未提交改动，提交时需要只暂存本任务相关文件/片段。
- 若“盘后筛选”与常规策略选股共用数据结构，应仅移除盘后筛选入口和独立流程，避免影响现有候选池展示。

# 关键决策记录

- 盘后筛选按独立产品流程移除；若底层函数还被其他策略复用，则优先保留公共函数，只移除对外入口和调度链路。
